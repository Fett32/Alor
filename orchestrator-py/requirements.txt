claude-agent-sdk>=0.1.60
prompt_toolkit>=3.0.0
# Drift-detector test for proto/alor_protocol.yaml (see
# test_protocol_drift.py). Small dep, pure Python, no native deps —
# fine to carry for a test-only use.
PyYAML>=6.0
